# jestPracticeMyFirstTest

# To use:

fork repo

npm init -y

npm install --save-dev jest

# to test the sum.test.js file, in the terminal write `npm run test` which is defined in the package.json file

```js
 "scripts": {
    "test": "jest"
  },
```

# looking forward to writing more tests.
